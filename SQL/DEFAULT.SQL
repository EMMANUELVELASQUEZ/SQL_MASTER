CREATE TABLE personas6 {
    id int NOT NULL,
    NAME VARCHAR (100) NOT NULL,
    AGE INT,
    EMAIL VARCHAR (50)DEFAULT CURRENT_TIMESTAMP(),
    CREATED DATATIME,
    UNIQUE (ID),
    PRIMARY KEY (ID),
    CHECK (AGE>= 18)

}